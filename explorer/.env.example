# Copy this file to explorer/.env and fill in your key.
# explorer/.env is gitignored — it never gets committed.
# The easy runner (python explorer/run.py) can also create this for you.
ANTHROPIC_API_KEY=sk-ant-...
